const { REQUIRED_PRIORITY, TASK_NAME, COLUMN_HEADINGS } = require('../constants/columnHeading.constant');

const {
  CATEGORY,
  FARM,
  CROP,
  FIELD_USER,
  FIELD_USER_ID,
  ASSIGNED_DATE,
  DETAILS,
  SPECIAL_INSTRUCTIONS,
  PRIORITY,
  FARM_ADDRESS,
  FARM_IMAGE,
  FARM_LOCATION,
  PLOT
} = COLUMN_HEADINGS;
const EXPECTED_COLUMNS = [CATEGORY, FARM, CROP, FIELD_USER, FIELD_USER_ID, ASSIGNED_DATE, DETAILS, SPECIAL_INSTRUCTIONS, PRIORITY];

const OPTIONAL_COLUMNS = [FARM_ADDRESS, FARM_LOCATION, FARM_IMAGE, PLOT];

const { FIELD_PREPERATION, SOWING, WEEDING, IRRIGATION, SPRAYING, HARVESTING, MAINTENANCE, OTHER } = TASK_NAME;
const ALLOWED_COLUMNS = [FIELD_PREPERATION, SOWING, WEEDING, IRRIGATION, SPRAYING, HARVESTING, MAINTENANCE, OTHER];

const { EMPTY_STRING, NORMAL, MODERATE, CRITICAL } = REQUIRED_PRIORITY;
const ALLOWED_PRIORITY = [EMPTY_STRING, NORMAL, MODERATE, CRITICAL, undefined];

module.exports = { EXPECTED_COLUMNS, ALLOWED_COLUMNS, ALLOWED_PRIORITY, OPTIONAL_COLUMNS };
